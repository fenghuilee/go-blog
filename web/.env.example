# API 基础 URL
# 开发环境通常使用代理，生产环境需要完整的 URL
# 如果为空或未设置，将使用默认值 '/api'
VITE_API_BASE_URL=/api

# 示例：
# 开发环境（使用 Vite 代理）: VITE_API_BASE_URL=/api
# 生产环境（完整 URL）: VITE_API_BASE_URL=https://api.example.com
# 本地开发（直接连接）: VITE_API_BASE_URL=http://localhost:8080/api
